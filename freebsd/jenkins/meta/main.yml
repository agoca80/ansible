---

dependencies:
  - role: freebsd
    freebsd_rc:
      jenkins_enable: "YES"
    freebsd_service: jenkins
    freebsd_packages:
      - awscli
      - bash
      - docker
      - helm
      - jenkins
      - git-lite
      - kubectl
      - py37-ansible
